# Optional. Leave empty to run without a localhost.run account.
# Set to /identity/<file> after you put a private key in ./identity and
# register its public key with localhost.run.
LOCALHOST_RUN_IDENTITY_FILE=
