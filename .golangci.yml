version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - copyloopvar
    - errname
    - errorlint
    - gocritic
    - gosec
    - misspell
    - nilerr
    - noctx
    - prealloc
    - testifylint
    - unconvert
    - unparam
    - usestdlibvars
    - wrapcheck
  settings:
    gosec:
      excludes:
        - G401
        - G501
  exclusions:
    generated: strict

formatters:
  enable:
    - gofumpt
    - gci
    - golines
  settings:
    gci:
      sections:
        - standard
        - default
        - prefix(github.com/nikitakarpei/yacy-rwi-node)
    golines:
      max-len: 100
