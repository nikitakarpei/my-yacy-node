// Package core groups the node's domain layer into three sibling roles:
// contracts holds the inbound interfaces the api depends on, services holds
// the domain logic fulfilling them, and ports holds the outbound interfaces
// infrastructure implements.
package core
