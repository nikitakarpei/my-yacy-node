// Package addressrealm is the shared vocabulary for saying which realm a
// thing belongs to. It names the two realms the operator configured, tells
// which one is the other, and names a crossing as the realm a seed or request
// comes from and the realm it enters. Every package that leases, marks,
// translates, or forwards per realm says so in these terms.
package addressrealm
