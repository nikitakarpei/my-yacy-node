// Package seedtranslation gives the seed one seed becomes when it enters a
// realm. The seed of a confirmed peer whose own translation stands keeps its
// hash and its network, reaches the realm at its leased address, and from a
// public bridge carries a translation mark; a seed that carries a bridge mark
// or a translation mark, and every other seed, becomes no seed at all.
package seedtranslation
