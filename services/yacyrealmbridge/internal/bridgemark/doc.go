// Package bridgemark owns the plain tag that a bridge's own seed carries in a
// realm, and tells whether a seed carries it. No bridge translates a seed with
// that tag, whoever set it.
package bridgemark
