// Package prefixaddresses is the translated address space of every address in
// one configured prefix, at one fixed port. It offers an address of that space
// that no lease has taken.
package prefixaddresses
