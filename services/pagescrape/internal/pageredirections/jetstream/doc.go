// Package jetstream remembers, in a key-value bucket this service owns, the URL each
// requested URL redirected to, so a corpus that holds a page under the URL the origin
// settled on still finds it under the URL somebody asked for.
package jetstream
